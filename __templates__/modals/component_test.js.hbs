import React from 'react';
import { Modal{{ pascalCase name }} }from './';
import { render } from '@testing-library/react-native';

describe('Render Modal{{ pascalCase name }}', () => {
  it('Should be Modal{{ pascalCase name }} exist', () => {
    const { getByTestId } = render(<Modal{{ pascalCase name }} />);
    const currentElement = getByTestId(`Modal{{ pascalCase name }}`);
    expect(currentElement).toBeTruthy();
  });
});